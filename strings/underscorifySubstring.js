// write a function that takes two strings. main string and a potential substring of the main string.
// the function should return a version of the main string with every instance of the substring wrapped in underscores


const underscorifySubstring = (string, substring) => {
    
}

let string = 'testthis is a testtest to see if testestest it works';
let substring = 'test'

// console.log(string.substr(14, 4));
console.log(underscorifySubstring(string, substring));
